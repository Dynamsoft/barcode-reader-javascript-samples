<style>
* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
  font-family: 'Segoe UI', Roboto, Helvetica, Arial, sans-serif, 'Microsoft YaHei', 'PingFang SC', 'Hiragino Sans GB', sans-serif;
}

html,
body {
  width: 100%;
  height: 100%
}

.home-page {
  position: absolute;
  width: 100%;
  height: 100%;
  background-color: #323234;
  z-index: 2;
}

.home-page .home-page-title {
  width: 100%;
  color: white;
  position: absolute;
  top: 35%;
  left: 50%;
  transform: translate(-50%, -50%);
  font-size: 30px;
  text-align: center;
}

.home-page .start-btn {
  height: 10%;
  min-height: 50px;
  border-radius: 50%;
  background-color: #ffffff;
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  aspect-ratio: 1/1;
  cursor: pointer;
  display: flex;
  justify-content: center;
  align-items: center;
}

.home-page .start-btn .start-icon {
  width: 0;
  height: 0;
  border-top: 15px solid transparent;
  border-bottom: 15px solid transparent;
  border-left: 25px solid #FE8E14;
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-40%, -50%);
}

.home-page .start-btn .loading {
  width: 70%;
  height: 70%;
  display: none;
  position: absolute;
  font-size: 14px;
  border: 4px solid #FE8E14;
  border-right-color: transparent;
  border-radius: 50%;
  color: transparent;
  font-weight: 700;
  animation: rotate 1s linear infinite;
}

.home-page .logo-dynamsoft {
  position: absolute;
  left: 50%;
  bottom: 50px;
  transform: translate(-50%, -50%);
  width: 130px;
  height: 32px;
}

.barcode-scanner-view {
  width: 100%;
  height: 100%;
}

.parsed-result-view {
  position: absolute;
  width: 100%;
  height: 100%;
  background-color: #DEDFE1;
  display: none;
  overflow: auto;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  border: 1px solid #E9EAEB;
}

.parsed-result-view .barcode-text {
  width: 100%;
  height: 80px;
  background-color: #fff;
  margin-bottom: 10px;
  color: #323234;
  padding: 10px 27px;
  font-weight: 600;
  overflow: auto;
  word-break: break-all;
}

.parsed-result-view .parsed-result-list {
  width: 100%;
  height: fit-content;
  max-height: 70%;
  background-color: #fff;
  overflow: auto;
}

.parsed-result-view .parsed-result-list .parsed-result-item {
  width: 100%;
  height: 60px;
  line-height: 60px;
  padding: 0 27px;
  border-bottom: 1px solid #E9EAEB;
  font-size: 18px;
  overflow: hidden;
  white-space: nowrap;
  text-overflow: ellipsis;
}

.parsed-result-view .parsed-result-list .parsed-result-item strong {
  font-weight: 500;
}

.parsed-result-view .parsed-result-list .parsed-result-item span {
  color: #666666;
}

.parsed-result-view .back-to-scan {
  width: 80%;
  height: 50px;
  max-width: 372px;
  background-color: #FE8E14;
  border-radius: 4px;
  color: #FFFFFF;
  font-size: 20px;
  text-align: center;
  line-height: 50px;
  margin: 30px auto;
}

@keyframes rotate {
  to {
    transform: rotate(360deg);
  }
}
</style>
<div class="home-page">
  <h1 class="home-page-title">Read a Driver's License</h1>
  <div class="start-btn">
    <div class="start-icon"></div>
    <div class="loading"></div>
  </div>
  <img class="logo-dynamsoft" src="./logo-dynamsoft.png" alt="logo-dynamsoft">
</div>
<div class="barcode-scanner-view">
  <Include src="https://cdn.jsdelivr.net/npm/dynamsoft-barcode-reader-bundle@11.6.3200/dist/ui/dce.ui.v5.xml">
</div>
<div class="parsed-result-view">
  <div class="barcode-text"></div>
  <ul class="parsed-result-list"></ul>
  <div class="back-to-scan" style="cursor: pointer;">Back to Scan</div>
</div>
<script>
  const camera = document.currentScript.currentDMCamera;
  const { CodeParserModule, CodeParser } = camera.dynamsoftExports;
  const { cvRouter, onDriverLicenseResult } = camera.uiContext;

  const parsedResultView = document.querySelector(".parsed-result-view");
  const homePage = document.querySelector(".home-page");
  const startIcon = document.querySelector(".start-icon");
  const startBtn = document.querySelector(".start-btn");
  const loading = document.querySelector(".loading");
  const barcodeText = document.querySelector(".barcode-text");
  const parsedResultList = document.querySelector(".parsed-result-list");
  const backToScan = document.querySelector(".back-to-scan");
  const barcodeScannerView = document.querySelector(".barcode-scanner-view");

  CodeParserModule.loadSpec(["AAMVA_DL_ID", "SOUTH_AFRICA_DL"]);

  let pParser;
  let pInit = (async () => {
    const parser = await (pParser ??= CodeParser.createInstance());
    await cvRouter.addResultReceiver({
      onDecodedBarcodesReceived: async (result) => {
        if (result.barcodeResultItems.length > 0) {
          let processedResult;
          try {
            const parsedResult = await parser.parse(result.barcodeResultItems[0].bytes);
            processedResult = handleDLResult(parsedResult);
          } catch (ex) {
            processedResult = [
              { description: "Error", value: ex.message || "parse failed." },
              {
                description: "Original text",
                value: result.barcodeResultItems[0].text,
              },
            ]
          } finally {
            onDriverLicenseResult(processedResult);
            generateResultList(processedResult);
            barcodeText.innerText = result.barcodeResultItems[0].text.replace(/\n/g, "");
          }
          parsedResultView.style.display = "block";
          barcodeScannerView.style.display = "none";
          camera.clearAllInnerDrawingItems();
          cvRouter.stopCapturing();
        }
      }
    });
    await cvRouter.initSettings("./read_dl.json");
  })();

  async function start() {
    await pInit;
    await camera.open();
    await cvRouter.startCapturing("ReadDriversLicense");
    homePage.style.display = "none";
  }

  function handleDLResult(parsedResult) {
    const oriParseInfo = [];
    const resultInfo = JSON.parse(parsedResult.jsonString);
    if (parsedResult.codeType === "AAMVA_DL_ID") {
      for (let info of resultInfo.ResultInfo) {
        if (info.FieldName !== "commonSubfile") continue;
        if (info.ChildFields) {
          wrapResultObject(info.ChildFields);
        }
      }
    } else if (parsedResult.codeType === "AAMVA_DL_ID_WITH_MAG_STRIPE") {
      for (let info of resultInfo.ResultInfo) {
        if (info.FieldName.includes("track")) {
          if (info.ChildFields) {
            wrapResultObject(info.ChildFields);
          }
        }
      }
    } else if (parsedResult.codeType === "SOUTH_AFRICA_DL") {
      for (let info of resultInfo.ResultInfo) {
        if (info.ChildFields) {
          wrapResultObject(info.ChildFields);
        } else {
          if (info.Value) {
            oriParseInfo.push({
              description: info.FieldName,
              value: info.Value,
            });
          }
        }
      }
    }

    function wrapResultObject(childFields) {
      for (let childField of childFields) {
        for (let field of childField) {
          if (!["dataElementSeparator", "segmentTerminator", "subfile", "subfileType"].includes(field.FieldName)) {
            if (field.Value) {
              oriParseInfo.push({
                description: field.FieldName,
                value: field.Value,
              });
            }
          }
          if (field.ChildFields) {
            wrapResultObject(field.ChildFields);
          }
        }
      }
    }
    return oriParseInfo;
  }

  function generateResultList(processedResult) {
    parsedResultList.innerHTML = "";
    const resultList = [];
    for (let item of processedResult) {
      const resultItem = document.createElement("li");
      resultItem.className = "parsed-result-item";
      resultItem.innerHTML = `<strong>${item.description}: </strong><span>${item.value}</span>`;
      resultList.push(resultItem);
    }
    parsedResultList.append(...resultList);
  }

  startBtn.addEventListener("click", () => {
    loading.style.display = "block";
    startIcon.style.display = "none";
    start();
  })

  backToScan.addEventListener("click", () => {
    parsedResultView.style.display = "none";
    barcodeScannerView.style.display = "block";
    start();
  })

  // Allow business logic to call `dispose()` to release resources.
  camera.uiContext.dispose = async()=>{
    (await pParser).dispose();
  };

</script>
